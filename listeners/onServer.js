import dotenv from "dotenv";
dotenv.config();

export default (io, socket) => {
    //player send message
    // socket.on(process.env.PLAYER_SEND_MESSAGE, (dataReceived) => {
    //     const data = { name: dataReceived.data.name, content: dataReceived.data.content };
    //     addPlayer(data);
    // });
};